## High

### [a/resource-management/apk-thumbnails/framework-vended-assetmanager-closed] The APK thumbnail fetcher closes an AssetManager the framework owns and caches

- **Location:** `app/src/main/java/com/mauriciotogneri/fileexplorer/data/util/ApkThumbnailFetcher.kt:148` (the close), with `:121` (the vended `Resources`)
- **Severity:** High
- **Confidence:** Medium
- **Defect:** `loadIconFromArchive` obtains its `Resources` from `packageManager.getResourcesForApplication(appInfo)` and then calls `resources.assets.close()` in its `finally`. That manager is not constructed by this app — `getResourcesForApplication` routes through `ActivityThread.getTopLevelResources` → `ResourcesManager.getOrCreateResources`, which registers the `ResourcesImpl` in the process-wide `mResourceImpls` map (keyed by the archive path, held by a `WeakReference`) and hands the *same* instance to every caller asking for that path. Closing it destroys a manager the framework still has registered and may still hand out. Two consequences follow from the one line:
  1. **Config-change crash.** The map entry is dropped only when the weak reference is *cleared*, i.e. after a GC — not when the manager is closed. Until then, any process-level configuration change makes `ResourcesManager.applyConfigurationToResources` iterate the map and call `updateConfiguration` on the destroyed impl, which reaches `AssetManager.setConfiguration` → `ensureValidLocked()` → `RuntimeException("AssetManager has been destroyed")` on the main thread. It is raised on an unrelated call stack long after `fetch()` returned, so the fetcher's own `catch (e: Exception)` cannot see it: the process dies.
  2. **Concurrent-fetch destruction.** Coil does not coalesce in-flight requests, and `extractApkIcon`/`loadIconFromArchive`/`rasterize` are entirely blocking, so a cancelled request keeps running to completion. Two overlapping fetches for the same archive therefore receive the identical cached `ResourcesImpl`; whichever finishes first destroys the manager under the other, which then fails inside `getDrawableForDensity`. That row falls back to the generic APK glyph, and because `isUnreadableApk` matches only `NameNotFoundException`, `Resources.NotFoundException` and an `ApplicationInfo` NPE, the bare `RuntimeException` is reported to Crashlytics instead of suppressed.
- **Trigger:** Browse a folder containing APKs (Downloads is the common one) on API 28+, then either (1) cause a configuration change — rotation, dark-mode toggle, locale, font size, display change — before the next GC clears the weak reference, or (2) scroll an APK row out of and back into view, or open the same APK at two sizes (list row vs Item Info preview), so two fetches for that path overlap.
- **Evidence / verification:** The close and its two guards are at `ApkThumbnailFetcher.kt:148-153`; the KDoc above it reasons explicitly about the framework's per-archive `Resources` cache, but only about the *lookup* path (`findResourcesImplForKeyLocked` rejecting a stale entry via `isUpToDate()`), which is correct and does not cover `applyConfigurationToResourcesLocked` walking the same map without a validity check. Baseline `git show 9e87306d:…/ApkThumbnailFetcher.kt` returns the `Drawable` directly and never touches `resources.assets`, so both failure modes are introduced. Refutation attempts that failed: the `SYSTEM_PACKAGE` guard covers only the framework's own package, not other archives; `FolderActivity`'s `android:configChanges` does **not** protect, because `ActivityThread` applies process-level resource configuration updates regardless of which changes an activity declares; and the app's own resources are not at risk, since that would need the user to browse `/data/app`, which is not listable. **Remaining assumption:** the final step rests on AOSP `ResourcesManager`/`ResourcesImpl`/`AssetManager` internals that are not in this repository and could not be executed here. Confirm on-device — rotate immediately after scrolling a folder of APKs — before spending effort on a fix. Note that the leak this close was written to fix (`ApkAssets.finalize() timed out`, itself a process kill) is real, so removing the close outright is not the answer.
- **Suggested fix:** Do not close a manager obtained from `getResourcesForApplication`. Build a private one for the archive instead — `AssetManager.Builder().addApkAssets(ApkAssets.loadFromPath(path))`, the pattern the other fetchers in this package already use — and close that. A privately constructed manager is registered nowhere and shared with nobody, which removes both the configuration-change window and the cross-fetch race at once.

## Medium

### [a/state-and-lifecycle/home-stored-entries/absent-volume-guard-bypassed-by-the-actions-sheet] Opening a favorite's or recent's action sheet on an unmounted volume permanently forgets the entry

- **Location:** `app/src/main/java/com/mauriciotogneri/fileexplorer/ui/screens/home/HomeViewModel.kt:701` (`showFavoriteActions`) and `:551` (`showRecentFileActions`); guard bypassed: `app/src/main/java/com/mauriciotogneri/fileexplorer/data/util/MountedStorage.kt:26` (`isForgettable`), applied at `HomeViewModel.kt:469-484`
- **Severity:** Medium
- **Confidence:** Medium
- **Defect:** This release added `isForgettable`, whose whole purpose is that `File.exists()` cannot tell "this file was deleted" from "this volume is not mounted", so an entry is forgotten only when its file is gone **and** its volume is currently mounted. Its KDoc states the stakes: "Forgetting one is permanent, and no volume coming back undoes it." Both action-sheet entry points still delete from the store on a bare `File(path).exists()` with no `mountedRoots` check. Because the release also deliberately leaves an ejected volume's cards on screen until something writes the store, the card the new guard keeps alive is exactly the card whose context menu destroys it.
- **Trigger:** Favorite a file on an SD card. Eject the card — `observeStorageVolumeChanges` fires `loadData(prune = false)`, which writes nothing, so the card is still rendered. Long-press or tap the overflow on that card. `exists()` is false, `removeFavorite(path)` rewrites the store without it, and reinserting the card does not bring it back. Recents behave the same way, though favorites are the durable case: `addRecentFile` writes on every file open, so recents are more likely to have been re-filtered already.
- **Evidence / verification:** Both call sites read verbatim `if (!fileExists) { favoritesRepository.removeFavorite(favorite.path) … }` / `recentFilesRepository.removeRecentFile(recentFile.path)`, with no `mountedRoots` parameter in scope. Refutation attempts that failed: the flow does not filter the card away first, because `favoritesFlow`/`recentFilesFlow` re-run `existingWithTimestamps()` only on a store emission and DataStore emits only on a write — and neither `loadData(prune = false)` nor a post-remount `loadData(prune = true)` writes (nothing is forgettable once the volume is back, so the `cleanedFiles.size != currentFiles.size` gate is false); a plain tap is *not* equivalent, since `openFavorite`/`openRecentFile` resolve to `StoredEntryDestination.Missing` and only toast; and `confirmDeleteFavorite` is a different path where the user did ask to forget the entry. **Attribution, stated plainly:** this is not a regression. The two `remove*` lines are byte-identical in the baseline, where the entry was doomed anyway by an unconditional prune on the next `ON_START`. The defect is that the guard this release introduced is partial, so the feature does not hold on a reachable interaction — a user who upgrades and believes their SD-card favorites now survive an eject will still lose them. A second consequence of the same root cause: when the volume is reinserted, nothing re-emits either flow, so entries filtered out earlier stay invisible until an unrelated store write or an app restart, contradicting the comment's promise that putting the card back restores them.
- **Suggested fix:** Have both call sites consult the same mounted-roots snapshot the prune uses (`storageRepository.getStorages().map { it.path }` plus `isForgettable`) before removing anything; when the path is merely on an absent volume, show the toast and leave the store untouched. Re-emitting the stores on a mount event would close the second half.

### [a/error-handling/file-delete/enoent-classified-as-already-absent] A delete that could not reach its target is reported as a success and may drive a prefix MediaStore delete

- **Location:** `app/src/main/java/com/mauriciotogneri/fileexplorer/data/util/FileAccess.kt:156`; consumed at `app/src/main/java/com/mauriciotogneri/fileexplorer/data/repository/FileRepository.kt:499-505` and `:660-666`, surfaced at `app/src/main/java/com/mauriciotogneri/fileexplorer/ui/screens/folder/FolderViewModel.kt:933-936`
- **Severity:** Medium
- **Confidence:** Medium
- **Defect:** `removePath` maps `ENOENT` to `RemoveOutcome.AlreadyAbsent` unconditionally. That is right for the case it was written for — another app removed the file first, and reporting a failure put an error in front of a user whose request was already satisfied. But `ENOENT` does not only mean "this file is gone": it is also what `remove(2)` returns when an **ancestor path component** is gone, and in that case the data may still exist. Such a node contributes no `failureErrno`, counts toward `deletedFiles`, and leaves the per-root gate `failedFiles == failedBefore && structuralFailures == structuralBefore` satisfied — so the walk reports a clean success. Worse, if anything under the root *was* genuinely unlinked before the path broke, `removedNodes > removedBefore` puts the root in `removedRootPaths`, which the caller hands to `MediaStoreUtil.notifyTreeDeleted` — a prefix row delete over a subtree this app did not empty, which is precisely what `RemoveOutcome`'s own KDoc says must never happen.
- **Trigger:** Any sequence where the path stops resolving while its files survive. Two concrete ones: (1) another app renames the parent folder part-way through a recursive delete, so the already-visited children are really gone but every remaining `remove(2)` — and the final `rmdir` — answers `ENOENT` while the files live on under the new name; (2) the volume is unmounted mid-delete. The `DeleteFailure.STORAGE_UNAVAILABLE` message this same change added is unreachable on both paths, because its errno set is the `ENOTCONN`/`ECONNABORTED`/`EIO`/`ENODEV`/`ENXIO` family, not `ENOENT`.
- **Evidence / verification:** The classification is `if (e.errno == OsConstants.ENOENT) RemoveOutcome.AlreadyAbsent else RemoveOutcome.Failed(e.errno)`. Baseline `deleteAndDropThumbnail` was `val deleted = file.delete()`, and `File.delete()` returns `false` for `ENOENT`, so the baseline classified every one of these nodes as a failure, showed the delete-failed toast and notified MediaStore of nothing — the misreported success is introduced. Refutation attempts that failed: `structuralDeleteFailed` does not catch it, because the directory's own `rmdir` also answers `ENOENT` and is classified `AlreadyAbsent`; and the `absentRootPaths`/`removedRootPaths` split does not save the mixed case, since a root needs only *one* genuinely unlinked node to be classified as removed. **Remaining assumption:** the unmount variant assumes an unmounted volume answers `ENOENT` rather than `ENOTCONN`/`EIO`, which was reasoned from how `vold` tears the mount point down and not measured on-device; the parent-rename variant reaches the same misreport with no device premise, so reachability does not rest on it. Harm is bounded — MediaProvider cannot unlink a file behind a path that no longer resolves, so this is a misreported success plus a purged media index, self-healing on the next scan, not data loss.
- **Suggested fix:** Distinguish "the target is absent" from "the path is unreachable" before trusting `ENOENT`. Stat the parent (or the volume root) when a node answers `ENOENT`: if the parent no longer resolves, classify the node `Failed` with that errno rather than `AlreadyAbsent`, so the walk reports the failure and the root never reaches `removedRootPaths`.

### [a/state-and-lifecycle/analyzer-category/delete-bookkeeping-lost-on-scope-cancellation] Leaving the category listing during a delete leaves the analyzer's results and chart claiming the deleted files

- **Location:** `app/src/main/java/com/mauriciotogneri/fileexplorer/ui/screens/analyzercategory/AnalyzerCategoryViewModel.kt:283-295`
- **Severity:** Medium
- **Confidence:** Medium
- **Defect:** `onDeleteConfirmed` launches on `viewModelScope` and does all of its bookkeeping *after* `fileRepository.delete(files)` returns. `delete` is `withContext(Dispatchers.IO) { … }` wrapping `deleteRecursive`, a plain blocking function with no `ensureActive()`, so once the body is entered the files are unlinked regardless of cancellation; cancellation is observed only when `withContext` resumes, which then throws. Everything after the call is therefore skipped: `MediaStoreUtil.notifyTreeDeleted`/`scanFiles`, `dropDeleted(...)` and with it `AnalyzerResultsHolder.remove`, and the analytics. The files are gone from disk but nothing else is told.
- **Trigger:** Confirm a delete in the category listing and press back before it finishes. There is deliberately no progress dialog and no `isDeleting` flag, so the whole screen including the back button stays live for the duration; the window is widest when a listed path has since become a directory, which the repository still deletes recursively.
- **Evidence / verification:** `viewModelScope.launch { val result = fileRepository.delete(files); … dropDeleted(...) }`, against `FileRepository.kt:440` (`withContext(Dispatchers.IO)`) and `:479-505` (`deleteRecursive`, no cancellation check). The repository already knows the caller's scope can die mid-delete — `notifyFilesMutated()` wraps its own callback in `withContext(NonCancellable)` — but the ViewModel side has no equivalent. Refutation attempts that failed: cancellation does not abort the unlink, for the reason above; and the folder screen is *not* an equivalent precedent, because its listing re-reads the directory from disk on re-entry and self-corrects, whereas the analyzer's listing and chart are backed by an in-memory holder that is never re-read. Consequences persist for the rest of the analyzer session: the chart still charges the freed bytes to that category and never brings `usedBytes` down, re-opening the slice draws ghost rows for files that no longer exist (`loadNextPage` does `FileItem.from(File(entry.path))` with no existence check), tapping one routes `IntentUtil.openFile` at a missing path, and MediaStore keeps rows for the deleted files.
- **Suggested fix:** Run the post-delete bookkeeping under `withContext(NonCancellable)`, the way `notifyFilesMutated` already does, so the holder update, the MediaStore notification and the analytics survive the scope being cancelled. Alternatively add `ensureActive()` inside `deleteRecursive` so cancellation actually stops the unlinking, and keep the bookkeeping proportionate to what was done.

### [b/contract-mismatches/analyzer-results/chart-capacity-and-breakdown-from-different-volumes] The results chart can be drawn against a different volume's capacity than it scanned

- **Location:** `app/src/main/java/com/mauriciotogneri/fileexplorer/ui/screens/analyzer/AnalyzerScreen.kt:151-153`; root cause at `app/src/main/java/com/mauriciotogneri/fileexplorer/ui/screens/analyzer/AnalyzerViewModel.kt:322-324` (`resolvedPath`) and `:146`
- **Severity:** Medium
- **Confidence:** Medium
- **Defect:** The results screen takes its numerator from one source and its denominator from another. `usedBytes` and `categories` are a snapshot of the volume that was **scanned**, captured at `startScan`; `totalBytes` and `usedFraction` are read live from `selectedStorage`, i.e. whichever volume is **currently selected**. Those are the same volume only while the selection cannot move — and `observeResults` moves it without user input: after a delete in the category listing it re-reads `getStorages()` and assigns `selectedPath = resolvedPath(it.selectedPath, storages)`. `resolvedPath` falls back to `storages.singleOrNull()?.path` when the previously selected path is no longer present, and nothing gates that fallback on the step, so it fires while `step` is still `RESULTS`.
- **Trigger:** On a two-volume device: scan the SD card, open a category, eject the card, delete a row (the files are now "already absent", so `dropDeleted` still runs and the holder still emits). `getStorages()` returns internal only, `resolvedPath` silently reselects it, and the ring centre now reports internal's capacity above the SD card's breakdown — e.g. "16% used / 20 GB / of 128 GB" with SD rows beneath. If internal's capacity is below the scanned used bytes, `coerceIn` pins the centre to "100% used". With three or more volumes, `singleOrNull()` returns null instead, so the centre reads "0% / of 0 B" and no arcs are drawn at all.
- **Evidence / verification:** `totalBytes = uiState.selectedStorage?.totalBytes ?: 0L` at the call site, `selectedStorage` = `storages.firstOrNull { it.path == selectedPath }`, and `usedFraction` divides the scanned `usedBytes` by that same live `totalBytes`. Refutation attempts that failed: the `step != RESULTS` guard at `AnalyzerViewModel.kt:123` is what *keeps* the screen on the results step — it returns early only when the step is not `RESULTS`, so it does not prevent the reassignment; the `freedBytes <= 0L` short-circuit sits *before* the re-read, and a real delete always frees more than zero; and I confirmed independently that `AnalyzerViewModel` observes no storage-volume-change source at all, so nothing else resets the step when a volume disappears.
- **Suggested fix:** Either carry the scanned volume's capacity in the ui state alongside `usedBytes`, so the whole chart is drawn from one snapshot, or gate the `singleOrNull()` fallback in `resolvedPath` on `step == SELECTION` and move the results screen to an explicit "the volume you scanned has gone" state when its path disappears.

## Low

### [b/contract-mismatches/storage-volume-kind/usb-drive-drawn-as-sd-card] A USB drive is labelled "USB Drive" and drawn with the SD-card icon

- **Location:** `app/src/main/java/com/mauriciotogneri/fileexplorer/data/source/AndroidStorageSource.kt:56-57` (`type`) and `:74-81` (`name`); `app/src/main/java/com/mauriciotogneri/fileexplorer/ui/components/StorageIcon.kt:10-13`
- **Severity:** Low
- **Confidence:** High
- **Defect:** `name()` consumes the new `genericKind` and returns `R.string.storage_usb_drive` for a volume the framework describes as a USB drive, but `type()` collapses every removable volume to `StorageType.SD_CARD`, and `StorageIcon` maps `SD_CARD` to `Icons.Outlined.SdCard`. `genericKind` is discarded before the UI sees it and is not carried on `StorageDevice`, so the two halves of the same row disagree.
- **Trigger:** Mount a USB OTG drive that the framework describes generically (i.e. one carrying no label of its own), then look at any list that draws a storage device: the home storage card, the analyzer volume card, or the destination picker.
- **Evidence / verification:** `private fun type(volume: Volume) = if (volume.isRemovable) StorageType.SD_CARD else StorageType.INTERNAL`, against `volume.info?.genericKind == GenericVolumeKind.USB_DRIVE -> context.getString(R.string.storage_usb_drive)`. `StorageType` has exactly two constants and `storageIcon` exactly two arms, so a USB volume necessarily draws the SD glyph. I resolved a direct conflict between two reviewers here: the claim that nothing in production resolves `storage_usb_drive` is wrong — `AndroidStorageSource.kt:78` resolves it and `VolumeInfo.kt:107` maps the framework kind. Refutation attempt that failed: `StorageType`'s KDoc justifies the collapse by claiming no public API reports a volume's disk type, but that premise is contradicted in the same file, since `genericKind` is trusted enough to pick the *name*. Baseline had neither `StorageType` nor `genericKind` and called the volume "SD Card" with the SD icon — internally consistent, merely mis-named — so the mismatch is new. The analytics side is not a regression: the baseline's path heuristic also reported `sd_card` for a USB drive.
- **Suggested fix:** Add a `USB_DRIVE` constant to `StorageType`, set it in `type()` from the same `genericKind` that `name()` already consults, and give `storageIcon` a matching arm (`Icons.Outlined.Usb`). That also lets `analyticsName` distinguish the two.

### [b/contract-mismatches/file-open-telemetry/no-handler-conflates-a-query-failure] A PackageManager query failure is filed as "the device has no app for this file"

- **Location:** `app/src/main/java/com/mauriciotogneri/fileexplorer/util/IntentUtil.kt:222-224`, with `hasLaunchableHandler`'s catch at `:327-330`
- **Severity:** Low
- **Confidence:** Medium
- **Defect:** `hasLaunchableHandler` returns `false` for two different things: the device genuinely having no launchable handler, and `queryIntentActivities` throwing. The new call site reads that single `false` as the former and files `file_open_failed` with `reason = "no_handler"`, whose own KDoc defines the value as "the device having no app for this file — nothing the app can start resolved it". The two causes are then indistinguishable in the dataset, which defeats the measurement the event was added to make, and a device where the query fails also files a Crashlytics non-fatal on every open-with.
- **Trigger:** "Open with" on a device where `packageManager.queryIntentActivities` throws — `TransactionTooLargeException`/`DeadObjectException` under PackageManagerService pressure, or an oversized result for the `*/*` wildcard query.
- **Evidence / verification:** `if (!hasLaunchableHandler(context, intent)) { trackFileOpenFailed(file, mimeType, source, "no_handler") }`, against a helper whose `catch (e: Exception)` reports `query_handlers` and returns `false`. Refutation attempt that failed on impact but succeeded on scope: user-visible behaviour is unaffected — the comment at `:217-221` is explicit that the probe must never gate the launch, and `IntentUtilOpenFileWithTest.noLaunchableHandler_stillLaunchesChooser` pins that the chooser launches regardless — so this is a telemetry-correctness defect only. The helper and its catch are pre-existing; the call site and the event that reads its result are new. **Remaining assumption:** how often the query throws in the field is unknown.
- **Suggested fix:** Have `hasLaunchableHandler` return a tri-state (or null on failure) so the call site can file a distinct `reason` such as `query_failed`, keeping `no_handler` to the case it documents.

### [a/logic-errors/analyzer-scan/used-total-not-refreshed-for-a-second-scan] A second scan measures against a storage snapshot that only in-listing deletes refresh

- **Location:** `app/src/main/java/com/mauriciotogneri/fileexplorer/ui/screens/analyzer/AnalyzerViewModel.kt:160-161`, against the one-shot read at `:92`
- **Severity:** Low
- **Confidence:** Medium
- **Defect:** `startScan` derives `usedBytes` as `storage.totalBytes - storage.availableBytes` from the `storages` list read once in `init`. That list is refreshed in only two places: the `StorageUnavailableException` recovery, and `observeResults` after a delete made *inside* the category listing. The code states the invariant it is protecting at `:135-139` — leaving the snapshot stale "would make a second scan of the volume start from a total too high by exactly the space the user deleted, and hand every one of those bytes to SYSTEM". That invariant is left open for a route the feature itself offers.
- **Trigger:** From the category listing, use `AnalyzerFileAction.OpenFolder` to jump into `FolderActivity`, delete files there, come back, and re-scan. `usedBytes` is computed from the pre-delete `availableBytes`, and `breakdown` hands the difference to `AnalyzerCategory.SYSTEM` — the exact wrong answer the comment names. Any other change in free space during the analyzer's lifetime does the same; since a scan takes minutes, a second scan is always well after the snapshot.
- **Evidence / verification:** `val usedBytes = storage.totalBytes - storage.availableBytes` in `startScan`, reading `_uiState.value.selectedStorage`, whose `storages` came from `init`. Refutation attempt that partly succeeded: every snapshot is stale to some degree, which is why this is Low — but the author documented the invariant and closed only one of the two in-app routes to breaking it. **Remaining assumption:** how often users take the OpenFolder route and then re-scan.
- **Suggested fix:** Re-read `storageRepository.getStorages()` at the head of `startScan` (it is already an async read elsewhere in this ViewModel) and derive `usedBytes` from that, rather than from the `init` snapshot.

### [b/resource-and-configuration-parity/analyzer-plurals/quantity-set-drift] The new `analyzer_found` plural declares quantities four languages cannot select and omits one three languages require

- **Location:** `app/src/main/res/values-es/strings.xml:478`, `values-fr/strings.xml:478`, `values-pt/strings.xml:478` (missing `many`); `values-in/strings.xml:444`, `values-ja/strings.xml:444`, `values-vi/strings.xml:444`, `values-zh/strings.xml:444` (surplus `one`)
- **Severity:** Low
- **Confidence:** Medium
- **Defect:** `analyzer_found` is the only plural this release adds whose quantity sets do not follow the convention already established in each locale's file. In Spanish, French and Portuguese every one of the 17 sibling plurals carries `many`, which CLDR selects for exact millions; `analyzer_found` carries only `one`/`other`, so a count of exactly 1,000,000 falls back to `other`. In Indonesian, Japanese, Vietnamese and Chinese — single-form languages whose other 17 plurals each carry `other` alone — it declares a `one` item that `PluralRules.select()` can never return.
- **Trigger:** `AnalyzerScreen.kt:359` calls `pluralStringResource(R.plurals.analyzer_found, fileCount, …)`. The es/fr/pt half needs a scan reporting exactly 1,000,000 (or 2,000,000, …) files in a category; the in/ja/vi/zh half is a resource that is dead on every count.
- **Evidence / verification:** Baseline quantity histograms confirm the drift is introduced — `git show 9e87306d:…/values-es/strings.xml` has 13 plurals with `13 many / 13 one / 13 other`, and HEAD has `17 many / 18 one / 18 other`, the odd one out being `analyzer_found`; `values-{in,ja,vi,zh}` go from `13 other` and nothing else to `1 one / 18 other`. Lint corroborates independently: `MissingQuantity` at exactly those three lines and `UnusedQuantity` at exactly those four. `LocalizationParityTest` cannot see either — its `requiredQuantities` map covers only `ar`/`ru`/`ro` and only checks for *missing* quantities, never surplus ones. Refutation attempt that partly succeeded and caps this at Low: I parsed all three files and confirmed `many` and `other` are byte-identical in all 17 sibling plurals in each of es, fr and pt, so following the convention would produce the same text and nothing user-visible differs today. **Remaining assumption:** that no future correction to those translations makes the two forms diverge. Neither half can throw; there is no crash here.
- **Suggested fix:** Add `many` to `analyzer_found` in es/fr/pt and drop the `one` item in in/ja/vi/zh. Widening `LocalizationParityTest.requiredQuantities` to `es`/`fr`/`pt` → `many` and adding a symmetric assertion that no plural declares a quantity its language cannot select would close both halves and stop the next one.

### [a/concurrency/uncompress-rollback/absent-path-reported-as-rolled-back] The extraction rollback can report a path it did not remove

- **Location:** `app/src/main/java/com/mauriciotogneri/fileexplorer/data/repository/FileRepository.kt:1496-1503`, with `deleteTree` at `:524`
- **Severity:** Low
- **Confidence:** Low
- **Defect:** The rollback is `if (created.exists() && deleteTree(created)) rolledBack.add(it)`, and `deleteTree(f)` is `deleteRecursive(f).failureErrno == null`. Since `AlreadyAbsent` carries no errno, a path that disappears between the `exists()` check and `removePath` makes `deleteTree` answer true, and the path joins `rolledBack` — which the caller feeds to a prefix MediaStore delete. That is exactly what the `exists()` guard was added to prevent, per the comment at `:1490-1495`.
- **Trigger:** Something else removes a just-extracted path in the microseconds between `exists()` and the `remove(2)` inside `deleteRecursive`, during a failed extraction's rollback.
- **Evidence / verification:** The guard and the `failureErrno == null` test as quoted. Baseline was `if (deleteRecursive(File(it))) rolledBack.add(it)`, and `File.delete()` answered `false` for an absent path, so the baseline did not have this hole — it had a different one, failing to report a directory that did come away when a child had been removed concurrently. Refutation attempt that nearly succeeded: the `exists()` guard genuinely narrows this to a microsecond window, and for any harm to follow, something must *also* take the path over before the MediaStore delete runs — it is a double race. **Remaining assumptions:** both races, neither of which could be provoked here.
- **Suggested fix:** Have `deleteTree` distinguish `Removed` from `AlreadyAbsent` and add to `rolledBack` only when something was actually unlinked, matching the `removedRootPaths`/`absentRootPaths` split the delete walk already makes.

### [a/boundary-and-encoding-cases/file-sorting/name-tiebreaker-ordering-mismatch] The size/date tiebreaker does not order names the way the name sort does

- **Location:** `app/src/main/java/com/mauriciotogneri/fileexplorer/data/repository/FileRepository.kt:246-247` (`thenByName`) against `:257-274` (`sortByNameInPlace`); the claim is at `:208-215`
- **Severity:** Low
- **Confidence:** Low
- **Defect:** `thenByName()` keys on `String.CASE_INSENSITIVE_ORDER` while `sortByNameInPlace` keys on `name.lowercase(Locale.ROOT)`, and the KDoc asserts the two are the same ordering ("matches what `sortByNameInPlace` keys on without the per-comparison `lowercase()` allocation"). They are not: `CASE_INSENSITIVE_ORDER` folds per `char` via `Character.toUpperCase`/`toLowerCase`, whereas `String.lowercase(Locale.ROOT)` applies SpecialCasing and can change a string's length.
- **Trigger:** A folder containing a name with a SpecialCasing character — `"İa"` lowercases to `"i̇a"`, so the name sort files it after `"ib"` while the size or date tiebreaker files it before. Visible as different row order for the same two files depending on the sort mode.
- **Evidence / verification:** The two key expressions as quoted; the baseline had no name tiebreaker on the size/date modes at all (`compareBy({ !it.isDirectory }, { it.size })`), so the divergence is introduced. Refutation attempts: for ASCII and Latin-1 the two orderings agree exactly, and the test `sortFiles orders equal-size folders the same way the name sort does` exercises only ASCII, so it cannot see this. Each comparator is internally consistent, so there is no transitivity violation and no `IllegalArgumentException` — the effect is row order only. **Remaining assumptions:** that any user has such a filename, and that they notice the ordering differs between modes.
- **Suggested fix:** Key both on the same thing. Either give `thenByName` a `lowercase(Locale.ROOT)` selector, or move the name sort to `CASE_INSENSITIVE_ORDER`, and correct the KDoc to name whichever is chosen.

### [a/state-and-lifecycle/analyzer-category/selection-not-pruned-when-entries-are-dropped] A row re-selected during a delete leaves a phantom selection with a dead Delete button

- **Location:** `app/src/main/java/com/mauriciotogneri/fileexplorer/ui/screens/analyzercategory/AnalyzerCategoryViewModel.kt:281` (the pre-launch `clearSelection()`) and `:343-363` (`dropDeleted`)
- **Severity:** Low
- **Confidence:** Low
- **Defect:** `onDeleteConfirmed` clears the selection before launching, and `dropDeleted` prunes `entries` and `state.files` but never `selectedPaths`. A path re-selected while its delete is in flight therefore survives in `selectedPaths` after its row has been removed from `files`.
- **Trigger:** Confirm a delete, then long-press one of the rows still on screen that is part of that in-flight delete. When the delete lands, `isSelectionMode` stays true and the top bar reads "1 selected" for a row that is not drawn; `selectedFiles` resolves to empty, so the bottom bar's Delete calls `showDeleteConfirmDialog(emptyList())`, `itemsToDelete` is empty, and `AnalyzerCategoryScreen.kt:296` never shows the dialog. The user is left with a stuck selection and a button that does nothing until they clear it by hand.
- **Evidence / verification:** `dropDeleted` updates `totalBytes`, `files` and `hasMore` and does not touch `selectedPaths`. That this is a recognised invariant in this codebase rather than an invented one is shown by `FolderViewModel.kt:531-536`, which documents leaving selection mode precisely so a selected path that no longer resolves to a listed file cannot "keep the screen counting a row it cannot show". Refutation attempts: the row menu is not a second route, since `FileListItem.kt:177` hides it in selection mode; `selectAll()` self-heals it, but only if the user happens to tap it; and the folder screen is immune because `loadFiles()` resets `selectedPaths` on every reload, which the analyzer listing never does. **Remaining assumptions:** the re-selection must land inside the delete window, which is usually brief.
- **Suggested fix:** Subtract `paths` from `state.selectedPaths` inside `dropDeleted`, so the selection can never outlive the rows it names.

### [b/contract-mismatches/analyzer-category/entry-cap-against-an-uncapped-total] A category can report itself empty while its chart row still shows bytes

- **Location:** `app/src/main/java/com/mauriciotogneri/fileexplorer/data/repository/AnalyzerRepository.kt:206` (`DEFAULT_MAX_ENTRIES_PER_TYPE = 10_000`) and `app/src/main/java/com/mauriciotogneri/fileexplorer/data/repository/AnalyzerResultsHolder.kt:66-79` (`remove`)
- **Severity:** Low
- **Confidence:** Low
- **Defect:** `CategoryFiles.totalBytes` is the category's full scanned total across the volume, while `CategoryFiles.entries` stops at the 10,000 largest files. `remove` subtracts only the bytes of the entries it drops, which is correct arithmetic but leaves a residual whenever the category held more files than the cap. Delete every listed entry and `AnalyzerCategoryUiState.isEmpty` becomes true while the chart row behind the listing still reports the bytes of the 10,001st file onward — a category presented as empty next to a slice claiming several GB, with no way to reach the remaining files.
- **Trigger:** A category holding more than 10,000 files (photos on a well-used device), then deleting all of the listed ones.
- **Evidence / verification:** The holder's KDoc reasons only about the subtraction ("subtracting its size is the same arithmetic either way") and does not address the residual. Refutation attempt that succeeded on the arithmetic but not on the state: both the ViewModel's `totalBytes` and the holder's are reduced by the same `removed.sumOf { it.size }`, computed independently over identical lists, so there is no double subtraction and no numeric error — this is an unreconciled presentation state, not bad maths. **Remaining assumptions:** reachability is low, since it needs both a category past the cap and the user deleting every listed row, which means paging through 100 pages.
- **Suggested fix:** Tell the listing how many files the category actually holds so it can distinguish "nothing left" from "nothing left that was listed", and show the residual explicitly rather than presenting a non-empty category as empty.

## Summary

**Findings by severity:** Critical 0 · High 1 · Medium 4 · Low 8 (13 total)

**Findings by confidence:** High 1 · Medium 7 · Low 5 (13 total)

| Severity | High | Medium | Low |
| --- | --- | --- | --- |
| Critical | 0 | 0 | 0 |
| High | 0 | 1 | 0 |
| Medium | 0 | 4 | 0 |
| Low | 1 | 3 | 4 |
